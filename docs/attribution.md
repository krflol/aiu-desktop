# Attribution

The Rust desktop adapter preserves the MIT-licensed Rust extraction contributed by
`krflol` in upstream PR #9 (commit `58fab73`). The adapter communicates with the
Go `aiu` sibling process and does not copy the Go credential store or its files.

The project remains available under the MIT license in [`LICENSE`](../LICENSE).
